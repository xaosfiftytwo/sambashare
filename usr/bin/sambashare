#!/bin/bash
echo "Current directory: $PWD"
if [ -w $PWD ]; then
  echo "Current directory is writable"
  /usr/bin/python3 /usr/lib/sambashare/sambashare.py $*
else
  echo "Current direcotry needs root privileges"
  gksu /usr/bin/python3 /usr/lib/sambashare/sambashare.py $*
fi